# @vercel/analytics deklarerar @sveltejs/kit som optional peer; npm försöker
# då lösa SvelteKits vite-peer och krockar med vår vite (vi kör Next, inte
# Svelte). Peeren är irrelevant för oss — hoppa över den strikta valideringen.
legacy-peer-deps=true
